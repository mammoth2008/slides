# CLAUDE.md

## 行为原则

**无知可以问，愚蠢不可接受。**

- 无知：不知道 → 查、问、学
- 愚蠢：知道但不说，为了快、"整洁"或讨好而给违心的错误答案 → 不接受

三条具体做法：
1. **分析什么结论就说什么结论**，不为求快或求"整洁"而修正判断。错了用户会纠正。
2. **读过的事实原样使用**。nav 标题、课程名、术语，读到什么就写什么，不许脑子里混入对话中的无关词汇。
3. **先调查再判断**。文件内容、截图、坐标，动手前先拿到事实，再分析，再行动。不要跳过调查阶段。

## 这是什么项目

基于 impress.js 的 3D 课程演示系统（HUST 经济管理学院）。多门课程共用同一套渲染框架，部署到 GitHub Pages（[slides.yemengma.cn](https://slides.yemengma.cn)）。

## 目录结构

| 路径 | 作用 |
|---|---|
| `/Users/Freeman/Documents/products/courses/library/texts/{课程}-slide-drafts/` | Markdown 源码（唯一源） |
| `{课程}/`（如 `10pmi/`、`5gai/`） | 生成的 HTML |
| `.claude/skills/slides-workflow/scripts/generate_html.py` | 统一 HTML 生成器；课程目录下 `*hg.py` 仅为兼容 wrapper |
| `.claude/skills/slides-workflow/` | 开发工作流（权威） |
| `.claude/skills/build-layout/` | 布局校验工具 |

**md 与 HTML 目录严格分离**：课程资料库 `library/texts/{课程}-slide-drafts/` 下不放生成后的 html，课程 HTML 目录下不放 md（教学大纲.md、章节结构.md、迁移期审查说明除外）。`draft/` 已移出发布根目录；归档副本位于 `archive/draft-20260704/`，只作回退参考，不再作为新工作的长期源。

**图片与资源目录规则**：课件使用的图片、SVG、mindmap 等前端资源，统一放在目标课程目录的 `img/cNN/` 下，其中 `NN` 为章节号，如第三章放 `5gai/img/c03/`，第四章放 `5gai/img/c04/`。md 中图片路径也写目标目录相对路径形式，如 `img/c04/example.svg`。

课程映射见 [slides-workflow/SKILL.md](.claude/skills/slides-workflow/SKILL.md) 的课程目录映射表。

## 文本主课件与图片化课件

课件结果页分为两类：

- **文本主课件**：以课程名前缀命名，如 `pmi-*`、`gai-*`、`dbpa-*`
- **图片化课件**：在对应文本主课件基础上进行图片化改造，文件名前缀为 `g{course}`，如 `gpmi-*`、`ggai-*`、`gdbpa-*`

两者关系如下：

- 文本主课件是结构与内容基线
- 图片化课件是视觉化改造版本，不改变 `md` 作为唯一源的地位
- 图片化课件通常沿用对应文本主课件的 slide 顺序、`mindmap`、`cXXq` 与练习页结构，再单独维护图片化链路导航

## a 系统与 c 系统

- `a系统` 指 `AGENTS.md` 与 `.agents/`
- `c系统` 指 `CLAUDE.md` 与 `.claude/`
- 两套系统的内容应保持一致；修改一侧相关说明、流程、规则时，另一侧要同步修改对应内容
- 两套系统的路径起点必须各自保留，不得把 `.agents/...` 改写成 `.claude/...`，也不得反向替换
- 判断是否一致时，以**规则、流程、说明内容一致**为准，不要求文件名和路径前缀相同

## 做任务时去哪儿

| 任务 | 入口 |
|---|---|
| 写/改某节 md、生成 HTML、修 cXXq | slides-workflow skill（会被自动触发） |
| 审查课程现状、建立重建起点 | course-audit skill |
| md 排版标点检查 | `.claude/skills/slides-workflow/references/markdown-sop.md` |
| 布局重叠/间距校验 | `python3 .claude/skills/build-layout/check_layout.py {html} --verbose` |
| 自动修标点空格 | `python3 .claude/skills/slides-workflow/chinese_fix.py {md}` |
| 生成 mindmap | slides-workflow skill 的 Mindmap 工作流 |

## 全局约定（会话开始就应知晓）

1. **md 是唯一源**。所有修改从课程资料库的 md 开始。
2. **生成器每次重置布局和 cXXq**。每次运行生成器后，必须重做移动、布局调整、cXXq 导航修复。
3. **未经用户审阅同意不生成 HTML**。流程是：完成 md → SOP 检查 → 等同意 → 生成。
4. **思考题只写在 cXXq 导航页**，不得单独成 slide；md 中不得出现 `### 思考题`。
5. **读过的原文原样写回**。nav 标题、章节名、术语名，读到什么写什么。
6. **生成器若先在旧 `draft/` 路径或课程资料库源包下产出 html，必须立即移动到目标课程目录**。不得复制后留下残留文件。

## 全局设计尺寸

所有课程的新视觉设计、封面背景、图片化课件和投影优先 QA，统一以 **1280×960（4:3）** 为主设计尺寸。

- 4:3 是当前课程投影全屏演示的主比例，优先保证投影面积最大化。
- `1280×960` 是新设计基准；`1024×768` 只作为旧框架/旧页面的兼容尺寸，不再作为新视觉系统的主目标。
- `1280×720` / 16:9 只作为网络复习时的浏览器兼容检查，不作为课件主设计比例。
- 背景图、封面图、图片化正文图默认按 `1280×960` 生产；如果使用更高分辨率，也必须保持 4:3。
- 后续重建课程时，应逐步把 HTML 的 `data-width/data-height`、CSS 安全区和图片资产统一到 1280×960 设计体系；已有 1024×768 页面改造时要明确标记为兼容或迁移。

## 课程设计方法论

工作方式是**瀑布式**：
1. 读教学大纲和章节结构
2. 定每节的核心主题
3. 围绕主题组织扩散内容
4. 选案例
5. 写 md

**案例选择**：同一类活动用不同方法管理，或同一情境用 PMBOK 框架解读。**禁止**拿不同类产品硬比（如微软 Windows vs 谷歌 Chrome）。

## 可复用经验

### 章节扩展

- **桥接节不提前讲完后续章节**：像 `3.3` 这类桥接节，应负责引出下一章的关键概念，而不是提前展开后续章节的全部内容。
- **总览章要持续回看总地图**：像 PMI 第五章这种按经典知识领域分节展开的总览章，写后续小节时要反复回看 `5.1` 的整体地图，避免只盯小节标题而漏掉本章应讲完的完整对象集合。
- **标题与内容边界不一致时，要在正文首页点明**：如果章节标题少写了一项、而课程设计或内容栏实际承担更多内容，正文第一页就应直接说明，不要指望学生从上下文自行推断。
- **课件表述要稍正式、体系化**：不要写成口语化、像随口列几个要点；即使保持简洁，也要有课程 slides 应有的结构感、判断框架和术语边界。
- **专业表述尽量上提一层**：不要只写"某专业学生如何如何"，优先改写成"AI 改变了什么更底层的流程"，这样更通用，也更容易形成课程主线。
- **数据获取与分析案例适合做桥梁案例**：它既能留在辅助编程语境里，又能自然过渡到 `workflow`、`skill` 和判断力。

### 图片使用

- **图片优先补抽象流程**：最适合加图的位置，是信息链条、执行闭环、数据流程、能力组成这类靠文字不易一眼看懂的内容。
- **没有合适现成图时，优先自绘 SVG**：SVG 清晰、易控、可长期复用，也避免外链失效和版权来源不稳。
- **图片页文字要长在场景里**：不要把正文后贴成说明框；标题、要点、标签应写在牌匾、站牌、票据、地图、任务板、说明纸等画面内实体上。
- **图片页观感先看“字是否属于这个场景”**：真正影响成败的，不只是字体是否好看，而是文字与画面语境是否一致。
- **案例图必须服务概念边界**：案例页不能只追求气氛或题材感；如果案例里同时存在两层对象，如 `礼制节点` 和 `管理阶段`，必须在图中显式区分。
- **图片化课件先做试片再量产**：优先先做 `2-3` 张试片，至少覆盖标题页、概念定义页、案例页三类页面，确认风格和文字关系无误后再全量生成。
- **图片化课件图片默认按 `1280×960` 设计**：这是所有课程的新主设计尺寸；要把原 slides 里的关键文本做得更大，帮助读者快速识别“这是需要读的内容”。
- **图片化课件不得用 SVG 重制原有 HTML 文本页**：图片化的目标是用 OpenAI `image 2` 模型生成高质量 `png`，完整替代原有 HTML 中正文 `div` 的文本内容；SVG 只可用于普通课件中的结构示意图、流程图等前端资源，不作为图片化课件正文页的重制方案。

### HTML 手改

- **只做局部增量时，可直接手改 HTML**：如果只是插图、补说明、微调措辞，且 slide 顺序和数量主结构不重排，可以不重生成。
- **手改 HTML 时，md 仍是唯一源**：即使这次不重生成，也要把相同结构同步写回 md，避免源和结果分叉。
- **替换图片资源时，同步修改 md 与结果 HTML 的路径**：如果这次只是把结果页里的图片从 `svg` 改成 `png`、或切换到新资源文件，而不立即重生成，也必须把源 `md` 里的同一图片路径一起改掉；否则下次生成会把结果覆盖回旧资源。
- **新增图片页可用卢曼式编号**：插在 `a5` 后的图片页可命名为 `a5a`，并保持与前页同视口，例如 `data-rel-to="a5"`、`rel-x=0`、`rel-y=0`、`rel-z=10`。
- **图片化课件结果页优先以对应文本主课件为骨架改写**：如 `g{course}-*.html` 优先从 `{course}-*.html` 转换，正文 slide 替成图片页，保留 `mindmap` 和 `cXXq`，再单独修本节左右导航。
- **图片化课件形成新链后，要回补上一节和总导航页**：除了当前节自身导航，还要修上一节图片化页面指向本节的链接，以及图片化总导航页中的课程入口。

### PMI 练习页

- **仅对 PMI 课程生效**：当编写或更新 `10pmi/` 下的分节 `exec html` 时，除从当前小节内容提炼题目外，还要检查 `3itpm/itpm-exec.html`
- **ITPM 题库只作补充源**：先保证题目服务于当前 `PMI` 小节，再从 `ITPM` 题库中挑选可直接迁移或轻度改写的题目
- **迁移标准必须严格**：只加入与当前小节概念同构、不会提前越界到后续章节、且不需要大幅改题干语境的题；不为凑题数硬加
- **边界复杂的小节更要少量补充**：像 `5.4` 这种同时覆盖风险、采购、干系人的小节，练习页应以本节内容为主，`ITPM` 只补少量同构题；不要把合同法、政府采购法等细节题整段搬入本节练习页。
- **题量按内容定，不固定 8 题**：单节练习页以内容完整和课堂节奏为准，`5-15` 题通常都可接受；不要机械凑成固定题数

## md 排版

权威文档：[.claude/skills/slides-workflow/references/markdown-sop.md](.claude/skills/slides-workflow/references/markdown-sop.md)

创建/修改 md 后**第一步**按 SOP 11 项清单检查。

**核心两条**（其余见 SOP）：
- 仅允许两种 slide：`### 标题 + 列表` 或 `![图](path) + #### 图片标题`
- 单 slide 超过 6-8 条列表时拆分，生成器不会自动拆

## 3D 布局

新设计以 1280×960（4:3）为主尺寸。既有页面中仍可能存在 1024×768 的旧 impress.js 坐标系，重建时应逐步迁移；如果暂时沿用旧坐标，要明确这是兼容处理。

校验规则（见 [check_layout.py](.claude/skills/build-layout/check_layout.py)）：

| 规则 | 阈值 |
|---|---|
| 概览可见 | x, y 均在 ±1500px |
| 同分支 y 间距 | ≥1000px |
| 分支走廊 x 间距 | ≥2000px |
| 任意两页不重叠 | 同 (x,y,z) 报 OVERLAP |

**分叉入口**：分叉第一页 `data-rel-to` 指向主干入口点（非前一分支页），`rel-y=0`。

**链式偏移陷阱**：只改 `data-rel-x` 不重定 `data-rel-to`，偏移会沿链条累加到几十万。改坐标时优先调 `rel-x`，分叉处显式重定 `rel-to`。

**图片 slide `rel-y=0` 是设计意图**（与前页同视口），check_layout 报的 OVERLAP 正常。

**布局规则有两套**：
- `standard`：所有分支第一页默认从标题页下方开始
- `wrap-title`：内容围绕标题页展开。靠近标题中心的分支第一页放在 `rel-y=1200`，远离标题中心的外侧分支第一页放在 `rel-y=-2000`
- `wrap-title` 判断内侧/外侧时，看分支的原始槽位 `x`，不是右侧视觉补偿后的最终 `x`
- `right_branch_extra_x=600` 只负责视觉补偿，不得参与 `wrap-title` 的内外侧分类
- 偶数分支（尤其 4 分支）要单独脑测：应先区分 `左外 / 左内 / 右内 / 右外`，再做右侧视觉补偿
- 如果只有 1 个内容分支，分支第一页默认不用 `rel-x=0`，而是用 `rel-x=400`，补偿标题居中、正文左对齐带来的视觉偏左
- 如果存在右侧分支，右侧分支默认额外向右补偿 `600px`，修正标题居中、正文左对齐造成的视觉不对称
- 新增或修改布局规则后，至少脑测 `1 / 2 / 3 / 4` 分支四种情况；校验输出若异常，必须回看写回后的 HTML 坐标，不只信脚本
- 起草布局草案时，除分组外，还应确认本节采用哪套布局规则；用户未指定时默认 `standard`

## HTML 手改 vs 重生成

**可直接改 HTML**（不用重生成）：改字、增删条目、改措辞——slide 数量和顺序不变即可。
**必须重生成**：slide 拆分、合并、改序、移动图片。

原因：生成器覆盖所有布局和 cXXq，重生成后全部重做。

**修改后重生成的标准流程**：
1. 先改课程资料库 `library/texts/{课程}-slide-drafts/` 下的源 `md`，必要时同步修改对应的 mindmap `md`
2. 运行统一生成脚本，默认直接写入目标课程目录；若旧兼容脚本把临时 `html` 落在旧 `draft/` 路径或课程资料库源包，立刻移动到目标课程目录
3. 重新应用 layout（至少执行一次 `--dry-run`，再正式写回）
4. 立即恢复 `cXXq` 三项内容：思考题、左右导航、中间练习链接
5. 如本节有独立练习页，同步检查并更新对应 `exec html`
6. 最后运行 `check_layout.py --verbose` 做结构校验

**重生成前先记住原有 `cXXq`**：如果这一节已经有稳定版本，重生成前先记录原有 `cXXq` 的思考题和前后导航，避免生成器重置后还要重新回忆和查找。

**修 `cXXq` 时优先按 `div id` 整块替换**：如果只是修改导航页，不要靠正文片段猜位置；应先定位 `id="cXXq"` 的整块 `div`，再整块替换思考题和左右导航。`-`、链接文案、空行都容易变，`div id` 才是稳定锚点。

**Edit 工具陷阱**：避免在 `</div>` 边界附近插入新 `<div>`（易误嵌套）。改结构时整块替换，不贴边插入。
- **图片化课件批量替换最容易坏在 `div` 边界**：如果第一次替换把 block 边界弄脏了，不要在脏结构上硬补，直接回到原始文本主课件页重新按 block 重写更稳。

## 常见技术坑

- **Python 字节码缓存**：改了 py 但运行旧代码 → 清 `__pycache__/`，仍不行用 Write 整体重写。
- **layout 脚本 div 末行格式**：`data-rel-z = {rz}>` 后必须有**一个空行**再写 slide 内容。漏 `>` 或漏空行 → impress.js 渲染失败。
- **`has_abs` 判断**：impress.js 绝对坐标需要 `data-x` 和 `data-y` 同时存在；只有 `data-z` 的（如 cXXq）不算绝对坐标。

## 部署与镜像

- 主站：[slides.yemengma.cn](https://slides.yemengma.cn)
- GitHub Pages：[mammoth2008.github.io/slides](https://mammoth2008.github.io/slides)
- Cloud 镜像：[slides.lifeplayer.club](https://slides.lifeplayer.club)
